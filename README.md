# The Office of Nils Johansson

My Personal website, Where I share about my life.

**Live:** [nj22az.github.io](https://nj22az.github.io)

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Markup | Semantic HTML5, multi-page |
| Styling | Self-contained CSS with dark/light design tokens |
| Scripts | Vanilla JavaScript, no frameworks |
| Typography | [Nunito](https://fonts.google.com/specimen/Nunito) (display) + system-ui (body) |
| Icons | Inline SVG paths (Lucide / SF Symbols aesthetic) |
| Logo | Inline SVG rubber-stamp seal (theme-adaptive via `currentColor`) |
| Comments | [Giscus](https://giscus.app) (GitHub Discussions) |
| Hosting | GitHub Pages (auto-deploys on push) |

**No build step.** No npm. No bundler. Just static files.

## Run Locally

```bash
npx serve .
# or
python3 -m http.server 8000
```

## Folder Structure

```
.
├── index.html              Main portal (hero, projects, journal, guestbook, about)
├── blog/index.html         Blog page (WordPress API + per-article Giscus comments)
├── config.js               All site data, projects, icons, logo, Giscus config
├── shared.js               Shared nav, footer, theme toggle, Giscus loader
├── styles.css              Complete stylesheet (dark-first design tokens)
├── _posts/                 Markdown blog post archive
├── assets/
│   ├── images/             Photos, app icons
│   └── video/              Video assets
├── CLAUDE.md               AI assistant instructions
├── .nojekyll               Tells GitHub Pages to skip Jekyll
└── .gitignore
```

## How Dark Mode Works

1. `<html data-theme="dark">` is set by default on every page
2. CSS uses custom properties scoped to `[data-theme="dark"]` and `[data-theme="light"]`
3. `shared.js` reads/writes `localStorage.getItem("nj-theme")` and updates the attribute
4. A sun/moon toggle button in the top nav switches the theme
5. Giscus comment widgets auto-sync when the theme changes
6. The SVG logo uses `currentColor`, so it adapts automatically

## How the Logo Works

The logo is a rubber-stamp seal SVG generated by `CONFIG.logo(size)` in config.js. It uses `currentColor` for all strokes and text, so it automatically matches the current theme. It's rendered:
- In the top navigation (32px) as the brand mark
- In the hero section (80px) as the centerpiece
- Hover interaction via the `.logo-seal` CSS class (subtle scale)

## How the Guestbook Works

The guestbook uses [Giscus](https://giscus.app), which maps to GitHub Discussions:
- Visitors sign in with their GitHub account to post
- Comments support markdown, reactions, and threaded replies
- Photos can be uploaded/attached directly in the comment editor
- The guestbook on the homepage uses `loadGiscus("#container", { term: "Guestbook" })`

### Enabling Giscus

1. Enable **Discussions** on the `nj22az/nj22az.github.io` repository
2. Create a "General" category (or your preferred name)
3. Go to [giscus.app](https://giscus.app) and enter your repo name
4. Copy the `data-repo-id` and `data-category-id` values
5. Paste them into `CONFIG.giscus.repoId` and `CONFIG.giscus.categoryId` in `config.js`

## How to Add a New Project

Edit **`config.js`** → `CONFIG.projects`:

```js
{
  title: "Project Name",
  description: "Short description.",
  url: "https://example.com",
  icon: "iconName",
  tags: ["Tag1", "Tag2"],
  featured: true,
}
```

Need a new icon? Add to `CONFIG.icons`:
```js
iconName: "M... SVG path data ...",
```

## How to Add a New Page

1. Create `new-page/index.html`
2. Use the same HTML shell as existing pages:
   - `<html lang="en" data-theme="dark">`
   - `<nav id="site-nav"></nav>` in body
   - `<footer id="site-footer" class="site-footer"></footer>`
   - Load `config.js` then `shared.js`
3. Navigation, footer, logo, and theme toggle are automatically injected

## Environment Variables

None. The site is entirely static.

## License

Copyright Nils Johansson. All rights reserved.
