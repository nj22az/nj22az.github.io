// Optional real-money shop. Keep disabled until the owner supplies and verifies the store mapping.
// Only a PUBLIC Storefront token belongs here. Never use an Admin or private Storefront token.
export const SHOPIFY_CONFIG=Object.freeze({enabled:false,domain:'',publicToken:'',apiVersion:'2026-07',products:{}});
